name: Architecture Closure Gate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  architecture-closure:
    name: Verify Architecture and Production Release Gates
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run architecture closure verifier
        run: python scripts/verify_architecture_closure.py

      - name: Run final three-state release gate verifier
        if: always()
        run: python scripts/verify_production_release_gate.py

      - name: Publish machine-readable architecture result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: architecture-closure-verification
          path: data/architecture-closure-verification.json
          if-no-files-found: error

      - name: Publish machine-readable production gate result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: production-release-gate
          path: data/production-release-gate.json
          if-no-files-found: error
